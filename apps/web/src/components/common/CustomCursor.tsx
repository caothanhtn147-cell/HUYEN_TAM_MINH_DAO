'use client';

import React from 'react';

// Neutralized to prevent stuck elements or lag; native OS cursor provides optimal 120 FPS performance
export const CustomCursor: React.FC = () => {
  return null;
};

export default CustomCursor;
